use studentapp;

CREATE TABLE users (
        id INT PEIMARY KEY AUTO_INCREMENT,
        name VARCHAR(20),
        email VARCHAR(100),
        website VARCHAR(255)
        gender VARCHAR(6)
        comment VARCHAR(100)
);